enum EmailSentScreenType { verification, resetPassword }

enum StockAlertType { critical, out, low }

enum MedicineListViewType { list, grid }

enum MedicineStockStatus { out, low, available }

enum ViewMode { list, grid }

enum FilterSelectionMode { single, multiple }

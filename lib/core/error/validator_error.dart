enum EmailInputError { empty, invalidFormat }

enum PasswordInputError { empty, tooShort }

enum ConfirmPasswordInputError { empty, mismatch }

enum FirstNameInputError { empty }

enum LastNameInputError { empty }

enum PhoneInputError { empty, invalidFormat }

enum PharmacyNameInputError { empty }

enum PharmacyLicenseInputError { empty }
